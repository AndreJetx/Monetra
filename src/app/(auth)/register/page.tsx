import { RegisterForm } from "@/features/identity/presentation/components/RegisterForm";

export const metadata = {
  title: "Cadastro",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
